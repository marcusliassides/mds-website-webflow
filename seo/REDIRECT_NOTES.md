Legacy news redirects
=====================

Webflow does not support wildcard 301 redirects in Hosting settings. After launch analytics
are available, catalog any legacy slugless news URLs and add them as explicit rows in
`seo/redirects.json`.
